import React from 'react';
import { Container, Typography } from '@mui/material';

const Urgences = () => {
    return (
        <Container>
            <Typography variant='h2' >Urgence Page</Typography>
        </Container>
    );
}

export default Urgences;
